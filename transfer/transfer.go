package transfer

import (
	"db-sync/config"
	"fmt"
)

func Transfer(localPath string, c *config.Config) (string, error) {
	fmt.Println("Transferring dump file...")

    // copy the file through scp
    

	return "", nil
}