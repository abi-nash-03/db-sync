## Sync your database
To sync database from one server to another


### The Go Workflow
```
# before committing
go fmt ./... # to format the code
go vet ./... # to check for errors
go test ./... # to run tests

# to produce a binary
go build -o db-sync .
```